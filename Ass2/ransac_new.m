function ransac_new(matches, f1, f2, N)

best_in_count = 0;

for i=1:N
    % pick random points
    perm = randperm(size(A,1));
    perm = perm(1:8);
    % Calculate F
    
    % Count inliers
    
    % Determine if better
    
    
end

end